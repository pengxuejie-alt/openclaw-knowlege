import { a as normalizeEnv, cn as getPositiveIntFlagValue, dn as hasFlag, fn as hasHelpOrVersion, in as normalizeWindowsArgv, l as enableConsoleCapture, ln as getPrimaryCommand, on as getCommandPath, r as isTruthyEnvValue, sn as getFlagValue, un as getVerboseFlag, v as defaultRuntime } from "./entry.js";
import { un as loadDotEnv } from "./auth-profiles-0NafhXwT.js";
import "./openclaw-root-jpxkdOqd.js";
import "./exec-DanmqEBd.js";
import "./agent-scope-DIyeRIwV.js";
import "./github-copilot-token-q57TYcbJ.js";
import "./host-env-security-VbAkcANc.js";
import { r as VERSION } from "./env-vars-DuCByIOF.js";
import "./manifest-registry-HCsle6hF.js";
import "./dock-BQCVKcZL.js";
import "./model-B0KrJA7k.js";
import "./pi-model-discovery-oH9Hk1Gw.js";
import "./frontmatter-aDb-YadT.js";
import "./skills-0mPuFOUn.js";
import "./path-alias-guards-C4tNGjPE.js";
import "./message-channel-CxHKaTgX.js";
import "./sessions-Bp3qR6Tw.js";
import "./plugins-m9MqVmdr.js";
import "./accounts-B2kIp6qA.js";
import "./accounts-DUsGdKnq.js";
import "./accounts-BNLQbNiM.js";
import "./bindings-ByCIQwuN.js";
import "./logging-Bgrm4o7g.js";
import "./send-DLu8xXnE.js";
import "./send-8sCdPPj_.js";
import "./subagent-registry-tIcCr1XE.js";
import "./paths-BvPCP-jf.js";
import "./chat-envelope-BbjUfSpL.js";
import "./client-BqUbcbV8.js";
import "./call-BP9ziIDj.js";
import "./pairing-token-SouSrG_r.js";
import "./net-gZV4PaxN.js";
import "./ip-DZF2tb1n.js";
import "./tailnet-DlV_mHNG.js";
import "./tokens-Dq9J__0M.js";
import "./with-timeout-PB1yrHJD.js";
import "./deliver-enaATHDX.js";
import "./diagnostic-3R7EyDo2.js";
import "./diagnostic-session-state-cwjDplnM.js";
import "./send-DJSkrf9G.js";
import "./image-ops-qGJz1hIz.js";
import "./pi-embedded-helpers-ZUlTEVbk.js";
import "./sandbox-D2rsqOoY.js";
import "./tool-catalog-C6QgTHG4.js";
import "./chrome-CMlRJr8W.js";
import "./tailscale-2WWUN5_D.js";
import "./auth-D6sRRUT0.js";
import "./server-context-dfzBVdwV.js";
import "./paths-C82DQ0ar.js";
import "./redact-6vvLhMiY.js";
import { r as formatUncaughtError } from "./errors-27FEsju9.js";
import "./fs-safe-Chvvdo1-.js";
import "./ssrf-CYyGTUfr.js";
import "./store-CZT5QwHx.js";
import "./ports-vw6wMYU9.js";
import "./trash-BXA_kzSK.js";
import "./server-middleware-LnMDQ9k2.js";
import "./tool-images-Cue9KtUK.js";
import "./thinking-Ds3Ekf1K.js";
import "./models-config-ChKtmyem.js";
import "./exec-approvals-allowlist-CDjewClL.js";
import "./exec-safe-bin-runtime-policy-Cbj7ouQo.js";
import "./reply-prefix-CbreE2me.js";
import "./memory-cli-DGSdjtIJ.js";
import "./manager-BVaYZmCG.js";
import "./gemini-auth-CIslOHue.js";
import "./fetch-guard-DHsIRI5K.js";
import "./query-expansion-X3vbva0F.js";
import "./retry-D7bSETth.js";
import "./target-errors-CBYoV-r2.js";
import "./chunk-ovaA0X4A.js";
import "./markdown-tables-Y10nk5Bh.js";
import "./local-roots-DE_WZCbt.js";
import "./ir-Bd8vY_Ao.js";
import "./render-CC7dS9Xb.js";
import "./commands-DMVnfDNZ.js";
import "./commands-registry-C75WkVal.js";
import "./image-CeDDzVBN.js";
import "./tool-display-BoRynF3H.js";
import { u as installUnhandledRejectionHandler } from "./runner-Ifho-pOe.js";
import "./model-catalog-DdcLYAX5.js";
import "./fetch-CLurgmdh.js";
import "./pairing-store-BT3ARjY_.js";
import "./exec-approvals-Cw71ZwnF.js";
import "./nodes-screen-BpGvBLA9.js";
import "./session-utils-Bv0cua6j.js";
import "./session-cost-usage-C2SjKcJX.js";
import "./skill-commands-Bi7kVsgu.js";
import "./workspace-dirs-CzC5BnHS.js";
import "./channel-activity-y6fRXZsT.js";
import "./tables-BVDOviE7.js";
import "./server-lifecycle-VjYMdfRu.js";
import "./stagger-DAKdJbmK.js";
import "./channel-selection-mS8vmtKZ.js";
import "./plugin-auto-enable-DMsYAgHe.js";
import "./send-DGkuppvL.js";
import "./outbound-attachment-ze8fTO9Q.js";
import "./delivery-queue-BQZr4-ta.js";
import "./send-C7dMdGs-.js";
import "./resolve-route-C9qMEAHm.js";
import "./system-run-command-VL-7vsc4.js";
import "./pi-tools.policy-18oy4THv.js";
import "./proxy-h0elQk1G.js";
import "./links-BXWkrGr8.js";
import "./cli-utils-BlFG2Sa_.js";
import "./help-format-GFuiXY9-.js";
import "./progress-CrCu56Pm.js";
import "./replies-1Cp-gYeg.js";
import "./onboard-helpers-B0Dw__Bi.js";
import "./prompt-style-BIi-Kp_L.js";
import "./pairing-labels-BQKfZTQ_.js";
import { t as ensureOpenClawCliOnPath } from "./path-env-DNDDCfCs.js";
import "./note-D1bhXJGh.js";
import { t as ensurePluginRegistryLoaded } from "./plugin-registry-DJI3SS56.js";
import { t as assertSupportedRuntime } from "./runtime-guard-Bxkrgx8-.js";
import { t as emitCliBanner } from "./banner-Cbei3WYF.js";
import "./doctor-config-flow-Bdeusjh4.js";
import { n as ensureConfigReady } from "./config-guard-aDZI4pxc.js";
import process$1 from "node:process";
import { fileURLToPath } from "node:url";

//#region src/cli/program/routes.ts
const routeHealth = {
	match: (path) => path[0] === "health",
	loadPlugins: true,
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const verbose = getVerboseFlag(argv, { includeDebug: true });
		const timeoutMs = getPositiveIntFlagValue(argv, "--timeout");
		if (timeoutMs === null) return false;
		const { healthCommand } = await import("./health-DbXTkU1b.js").then((n) => n.i);
		await healthCommand({
			json,
			timeoutMs,
			verbose
		}, defaultRuntime);
		return true;
	}
};
const routeStatus = {
	match: (path) => path[0] === "status",
	loadPlugins: true,
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const deep = hasFlag(argv, "--deep");
		const all = hasFlag(argv, "--all");
		const usage = hasFlag(argv, "--usage");
		const verbose = getVerboseFlag(argv, { includeDebug: true });
		const timeoutMs = getPositiveIntFlagValue(argv, "--timeout");
		if (timeoutMs === null) return false;
		const { statusCommand } = await import("./status-B9cT0x4u.js").then((n) => n.t);
		await statusCommand({
			json,
			deep,
			all,
			usage,
			timeoutMs,
			verbose
		}, defaultRuntime);
		return true;
	}
};
const routeSessions = {
	match: (path) => path[0] === "sessions" && !path[1],
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const allAgents = hasFlag(argv, "--all-agents");
		const agent = getFlagValue(argv, "--agent");
		if (agent === null) return false;
		const store = getFlagValue(argv, "--store");
		if (store === null) return false;
		const active = getFlagValue(argv, "--active");
		if (active === null) return false;
		const { sessionsCommand } = await import("./sessions-BouLlNd0.js").then((n) => n.n);
		await sessionsCommand({
			json,
			store,
			agent,
			allAgents,
			active
		}, defaultRuntime);
		return true;
	}
};
const routeAgentsList = {
	match: (path) => path[0] === "agents" && path[1] === "list",
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const bindings = hasFlag(argv, "--bindings");
		const { agentsListCommand } = await import("./agents-i4OTRqax.js").then((n) => n.t);
		await agentsListCommand({
			json,
			bindings
		}, defaultRuntime);
		return true;
	}
};
const routeMemoryStatus = {
	match: (path) => path[0] === "memory" && path[1] === "status",
	run: async (argv) => {
		const agent = getFlagValue(argv, "--agent");
		if (agent === null) return false;
		const json = hasFlag(argv, "--json");
		const deep = hasFlag(argv, "--deep");
		const index = hasFlag(argv, "--index");
		const verbose = hasFlag(argv, "--verbose");
		const { runMemoryStatus } = await import("./memory-cli-DGSdjtIJ.js").then((n) => n.t);
		await runMemoryStatus({
			agent,
			json,
			deep,
			index,
			verbose
		});
		return true;
	}
};
function getCommandPositionals(argv) {
	const out = [];
	const args = argv.slice(2);
	for (const arg of args) {
		if (!arg || arg === "--") break;
		if (arg.startsWith("-")) continue;
		out.push(arg);
	}
	return out;
}
function getFlagValues(argv, name) {
	const values = [];
	const args = argv.slice(2);
	for (let i = 0; i < args.length; i += 1) {
		const arg = args[i];
		if (!arg || arg === "--") break;
		if (arg === name) {
			const next = args[i + 1];
			if (!next || next === "--" || next.startsWith("-")) return null;
			values.push(next);
			i += 1;
			continue;
		}
		if (arg.startsWith(`${name}=`)) {
			const value = arg.slice(name.length + 1).trim();
			if (!value) return null;
			values.push(value);
		}
	}
	return values;
}
const routes = [
	routeHealth,
	routeStatus,
	routeSessions,
	routeAgentsList,
	routeMemoryStatus,
	{
		match: (path) => path[0] === "config" && path[1] === "get",
		run: async (argv) => {
			const pathArg = getCommandPositionals(argv)[2];
			if (!pathArg) return false;
			const json = hasFlag(argv, "--json");
			const { runConfigGet } = await import("./config-cli-BcRxsKxn.js");
			await runConfigGet({
				path: pathArg,
				json
			});
			return true;
		}
	},
	{
		match: (path) => path[0] === "config" && path[1] === "unset",
		run: async (argv) => {
			const pathArg = getCommandPositionals(argv)[2];
			if (!pathArg) return false;
			const { runConfigUnset } = await import("./config-cli-BcRxsKxn.js");
			await runConfigUnset({ path: pathArg });
			return true;
		}
	},
	{
		match: (path) => path[0] === "models" && path[1] === "list",
		run: async (argv) => {
			const provider = getFlagValue(argv, "--provider");
			if (provider === null) return false;
			const all = hasFlag(argv, "--all");
			const local = hasFlag(argv, "--local");
			const json = hasFlag(argv, "--json");
			const plain = hasFlag(argv, "--plain");
			const { modelsListCommand } = await import("./models-DMHG5iLY.js").then((n) => n.t);
			await modelsListCommand({
				all,
				local,
				provider,
				json,
				plain
			}, defaultRuntime);
			return true;
		}
	},
	{
		match: (path) => path[0] === "models" && path[1] === "status",
		run: async (argv) => {
			const probeProvider = getFlagValue(argv, "--probe-provider");
			if (probeProvider === null) return false;
			const probeTimeout = getFlagValue(argv, "--probe-timeout");
			if (probeTimeout === null) return false;
			const probeConcurrency = getFlagValue(argv, "--probe-concurrency");
			if (probeConcurrency === null) return false;
			const probeMaxTokens = getFlagValue(argv, "--probe-max-tokens");
			if (probeMaxTokens === null) return false;
			const agent = getFlagValue(argv, "--agent");
			if (agent === null) return false;
			const probeProfileValues = getFlagValues(argv, "--probe-profile");
			if (probeProfileValues === null) return false;
			const probeProfile = probeProfileValues.length === 0 ? void 0 : probeProfileValues.length === 1 ? probeProfileValues[0] : probeProfileValues;
			const json = hasFlag(argv, "--json");
			const plain = hasFlag(argv, "--plain");
			const check = hasFlag(argv, "--check");
			const probe = hasFlag(argv, "--probe");
			const { modelsStatusCommand } = await import("./models-DMHG5iLY.js").then((n) => n.t);
			await modelsStatusCommand({
				json,
				plain,
				check,
				probe,
				probeProvider,
				probeProfile,
				probeTimeout,
				probeConcurrency,
				probeMaxTokens,
				agent
			}, defaultRuntime);
			return true;
		}
	}
];
function findRoutedCommand(path) {
	for (const route of routes) if (route.match(path)) return route;
	return null;
}

//#endregion
//#region src/cli/route.ts
async function prepareRoutedCommand(params) {
	emitCliBanner(VERSION, { argv: params.argv });
	await ensureConfigReady({
		runtime: defaultRuntime,
		commandPath: params.commandPath
	});
	if (params.loadPlugins) ensurePluginRegistryLoaded();
}
async function tryRouteCli(argv) {
	if (isTruthyEnvValue(process.env.OPENCLAW_DISABLE_ROUTE_FIRST)) return false;
	if (hasHelpOrVersion(argv)) return false;
	const path = getCommandPath(argv, 2);
	if (!path[0]) return false;
	const route = findRoutedCommand(path);
	if (!route) return false;
	await prepareRoutedCommand({
		argv,
		commandPath: path,
		loadPlugins: route.loadPlugins
	});
	return route.run(argv);
}

//#endregion
//#region src/cli/run-main.ts
function rewriteUpdateFlagArgv(argv) {
	const index = argv.indexOf("--update");
	if (index === -1) return argv;
	const next = [...argv];
	next.splice(index, 1, "update");
	return next;
}
function shouldSkipPluginCommandRegistration(params) {
	if (params.hasBuiltinPrimary) return true;
	if (!params.primary) return hasHelpOrVersion(params.argv);
	return false;
}
function shouldEnsureCliPath(argv) {
	if (hasHelpOrVersion(argv)) return false;
	const [primary, secondary] = getCommandPath(argv, 2);
	if (!primary) return true;
	if (primary === "status" || primary === "health" || primary === "sessions") return false;
	if (primary === "config" && (secondary === "get" || secondary === "unset")) return false;
	if (primary === "models" && (secondary === "list" || secondary === "status")) return false;
	return true;
}
async function runCli(argv = process$1.argv) {
	const normalizedArgv = normalizeWindowsArgv(argv);
	loadDotEnv({ quiet: true });
	normalizeEnv();
	if (shouldEnsureCliPath(normalizedArgv)) ensureOpenClawCliOnPath();
	assertSupportedRuntime();
	if (await tryRouteCli(normalizedArgv)) return;
	enableConsoleCapture();
	const { buildProgram } = await import("./program-B3840brE.js");
	const program = buildProgram();
	installUnhandledRejectionHandler();
	process$1.on("uncaughtException", (error) => {
		console.error("[openclaw] Uncaught exception:", formatUncaughtError(error));
		process$1.exit(1);
	});
	const parseArgv = rewriteUpdateFlagArgv(normalizedArgv);
	const primary = getPrimaryCommand(parseArgv);
	if (primary) {
		const { getProgramContext } = await import("./program-context-Cd1EJIKv.js").then((n) => n.n);
		const ctx = getProgramContext(program);
		if (ctx) {
			const { registerCoreCliByName } = await import("./command-registry-etkeRU0O.js").then((n) => n.t);
			await registerCoreCliByName(program, ctx, primary, parseArgv);
		}
		const { registerSubCliByName } = await import("./register.subclis-CfO22oox.js").then((n) => n.a);
		await registerSubCliByName(program, primary);
	}
	if (!shouldSkipPluginCommandRegistration({
		argv: parseArgv,
		primary,
		hasBuiltinPrimary: primary !== null && program.commands.some((command) => command.name() === primary)
	})) {
		const { registerPluginCliCommands } = await import("./cli-CxlSD0PO.js");
		const { loadConfig } = await import("./auth-profiles-0NafhXwT.js").then((n) => n.D);
		registerPluginCliCommands(program, loadConfig());
	}
	await program.parseAsync(parseArgv);
}

//#endregion
export { runCli };